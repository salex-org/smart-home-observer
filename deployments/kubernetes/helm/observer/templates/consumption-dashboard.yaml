{{- if .Values.config.grafana.enabled }}
apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: {{ .Values.name }}-consumption-dashboard
  namespace: {{ .Release.Namespace }}
spec:
  folderRef: {{ .Values.name }}-parent-folder
  resyncPeriod: 30s
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      grafana: naboo
  json: >
{{ .Files.Get "dashboards/consumption-dashboard.json" | indent 4 }}
{{- end }}