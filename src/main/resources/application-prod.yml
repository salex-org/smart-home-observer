spring:
  datasource:
    url: 'jdbc:derby:database;create=true;user=APP'
  jpa:
    hibernate:
      ddl-auto: update
  mail:
    host: 'smtp.1und1.de'
    port: 587
    username: '{cipher}'
    password: '{cipher}'
    properties:
      mail:
        smtp:
          auth: true
          timeout: 3000
          connectiontimeout: 5000
          writetimeout: 5000
          starttls:
            enabled: true
org:
  salex:
    raspberry:
      script:
        cpu: '/usr/bin/vcgencmd'
    cron:
      measure: '0 0/1 * * * *'
#      measure: '0 5/10 * * * *'
#      statistics: '0 0 0 * * *'
#      alert: '0 0 6 * * *'
    blog:
      url: 'https://holzwerken.salex.org/wp-json/wp/v2'
      username: '{cipher}'
      password: '{cipher}'
    mail:
      climateAlertAddresses:
        - 'sascha.gaertner@salex.org'
    hmip:
      client:
        access-point-sgtin: '{cipher}'
        device-id: '{cipher}'
        client-id: '{cipher}'
        client-name: 'Smart Home Observer'
        client-auth-token: '{cipher}'
        auth-token: '{cipher}'
